#include "lists.h"
/**
  * print_listint_safe - function that prints a
  * listint_t linked list.
  * @head: print lists with a loop
  * Return: the number of nodes in the list
  */
size_t print_listint_safe(const listint_t *head)
{
size_t i = 0;

(void) head;
return (i);
}

