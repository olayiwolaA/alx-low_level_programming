#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the name of this program
 * @argc: length of the arguments
 * @argv: display the arguments
 * Return: 0
 */

int main(int argc, char **argv)
{
if (argc == 1)
printf("%s\n", *argv);

return (0);
}

