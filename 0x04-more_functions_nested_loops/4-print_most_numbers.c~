#include "main.h"
/**
 *  prints the numbers, from 0 to 9, Do not print 2 and 4.
 *
 * Return: Always 0.
 */

void print_most_numbers(void)
{
char print_num;
for (print_num = 0; print_num < 10; print_num++)
{
if (print_num != 2 && print_num != 4)
_putchar(print_num + '0');
}
_putchar('\n');
}
