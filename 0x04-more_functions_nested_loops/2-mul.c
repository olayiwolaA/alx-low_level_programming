#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @s: is a int variable
 * @e: is a int variable
 * Return: Always 0.
 */
int mul(int s, int e)
{
int mul;
mul = s * e;
return (mul);
}
