#include "main.h"
/**
 * puts2 - print other character of a string, starting with the first character.
 * @str: pointer int type
 * Return: Always 0.
 */
void puts2(char *str)
{
int i;
for (i = 0; *str != '\0'; i++)
{
if (i % 2 == 0)
_putchar(*str);
str++;
}
_putchar('\n');
}
